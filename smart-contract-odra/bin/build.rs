fn main() {
    // This is a placeholder for the Odra build system
    // Use `cargo odra build -c StakeVue` to build the contract
    println!("Use 'cargo odra build -c StakeVue' to build the contract");
}
